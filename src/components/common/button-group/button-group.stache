<div class="btn-group">
  {{#each buttons as button}}
    <a href="#" on:click="select(button)" class="btn btn-default btn-sm {{#if button.isSelected}}btn-selected{{/if}}">{{button.name}}</a>
  {{/each}}
</div>